{
  "name": "livemarks",
  "displayName": "Livemarks",
  "description": "Semi-WYSIWYG markdown styling: bold appears bold, italic italic, headings scale — right in the editor.",
  "version": "0.1.0",
  "engines": { "vscode": "^1.85.0" },
  "categories": ["Other"],
  "activationEvents": ["onLanguage:markdown"],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "Livemarks",
      "properties": {
        "livemarks.syntaxMarkers": {
          "type": "string",
          "enum": ["dimmed", "hidden"],
          "enumDescriptions": [
            "Syntax markers (**, *, ##, ~~) are always visible at reduced opacity.",
            "Syntax markers are invisible on all lines except the one containing the cursor."
          ],
          "default": "hidden",
          "description": "Controls how markdown syntax markers are displayed in the editor."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "test:unit": "vitest run"
  },
  "devDependencies": {
    "@types/markdown-it": "^14.1.2",
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.3.0",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "markdown-it": "^14.1.0"
  }
}
